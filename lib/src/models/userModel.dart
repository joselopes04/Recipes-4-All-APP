class UserModel{

  String? uid, username, photoUrl, email;
  bool isAnonymous;

  UserModel({ required this.uid, required this.isAnonymous,required this.email, required this.username,  photoUrl});

}